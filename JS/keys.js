// import { player } from "./player.js";
document.addEventListener('keydown', e => {

    // if (e.key === 'ArrowRight') { player.x += 1; console.log(player); frameX += 1};

    if (e.key === 'Enter') { console.log("Hello") };
    // if(e.key === e.key) {console.log(e)}
});